<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.darshana.moneymanager"

    >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">

        <activity android:name=".MainActivity"
            android:resizeableActivity="true"
            android:theme="@style/HomePage"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Organization"
            android:parentActivityName=".MainActivity"
            android:label="@string/selectOrg"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".NewOrganization"
            android:parentActivityName=".Organization"
            android:label="@string/addNewOrg"
            android:theme="@style/AppTheme"
            >
        </activity>

    </application>

</manifest>